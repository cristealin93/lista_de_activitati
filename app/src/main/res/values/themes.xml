<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ListăDeActivități" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Base application theme. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/barAndFooterColor</item>
        <item name="colorPrimaryVariant">@color/barAndFooterColor</item>
        <item name="colorOnPrimary">@color/barAndFooterColor</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/barAndFooterColor</item>
        <item name="colorSecondaryVariant">@color/barAndFooterColor</item>
        <item name="colorOnSecondary">@color/barAndFooterColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnPrimary</item>
        <item name="android:navigationBarColor">@color/barAndFooterColor</item>

        <!-- Customize your theme here. -->
    </style>


    <style name="AppTheme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:statusBarColor">@color/fap_button</item>
        <item name="android:navigationBarColor">@color/fap_button</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AppTheme.mainActivity" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:statusBarColor">@color/barAndFooterColor</item>
        <item name="android:navigationBarColor">@color/barAndFooterColor</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>


</resources>